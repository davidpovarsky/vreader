---
kind: bug
id: 179
status_target: FIXED
commit_sha: 8c08df77171b024788c062307dae7c24ee9a5eb9
app_version: 3.21.31 (build 308)
date: 2026-05-14
verifier: claude
device_or_simulator: iPhone 17 Pro Simulator
os_version: iOS 26.4
build_configuration: Debug
backend: n/a (UI-only fix)
result: pass
---

# Bug #179 — TXT Dynamic Island Clipping (post-merge close-gate verification)

## Acceptance criteria

| Criterion | Observed | Result |
|---|---|---|
| First line of TXT content visible below Dynamic Island on iPhone 17 Pro | Title "War and Peace" renders at screen y≈270 (in 1206-wide screen coords); DI ends at y≈180; clear ~90pt gap | PASS |
| Same fix applies to MD reader (uses same TXT bridge) | MD scroll mode wraps `readerContent` in `GeometryReader` identically | PASS (by code review; MD pre-FIXED simulator check covered same bridge) |
| Chunked TXT path (large files via `TXTChunkedReaderBridge`) compensates safe-area | `chunkedReaderContent` wraps in `GeometryReader`, passes `safeAreaTopInset` into bridge; bridge sets `tableView.contentInset.top = max(0, safeAreaTopInset)` + `contentInsetAdjustmentBehavior = .never` | PASS |
| Devices without Dynamic Island unaffected | `combinedTextInset(base:safeAreaTop:0) == base` (unit test `zeroSafeAreaPreservesBase`) | PASS |
| No regression in existing TXT reader behavior | Full vreaderTests Swift Testing run: 805/805 pass; XCTest flakes unchanged (`AutoPageTurnerTests`, `TTSServiceSpeedControlTests` — same set as bug #167 close-gate note) | PASS |

## Commands run

```bash
# Build merged-build at v3.21.31 (commit 8c08df7)
git checkout main && git pull
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild build \
    -project vreader.xcodeproj -scheme vreader \
    -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -configuration Debug

# Install on iPhone 17 Pro Simulator
APP_PATH=$(find ~/Library/Developer/Xcode/DerivedData/vreader-hdhlhcqmxppsadhececcxeadpkvz/Build/Products/Debug-iphonesimulator -maxdepth 2 -name "vreader.app" -type d | head -1)
xcrun simctl install booted "$APP_PATH"
xcrun simctl launch booted com.vreader.app

# Seed war-and-peace TXT fixture, open it
xcrun simctl openurl booted "vreader-debug://reset"
xcrun simctl openurl booted "vreader-debug://seed?fixture=war-and-peace"
xcrun simctl openurl booted "vreader-debug://open?bookId=txt:bd8285a80f01df96dedd20a02178043afb85c0b499127e300baf57b7f1ed7508:1705"

# Screenshot
xcrun simctl io booted screenshot dev-docs/verification/artifacts/bug-179-postmerge-txt-di-clear-20260514.png
```

## Observations

- Title "War and Peace" sits clearly below the Dynamic Island after the fix. The Dynamic Island
  occupies ~59pt of safe-area top; the static helper `combinedTextInset(base:safeAreaTop:)` sums
  the typographic base (16pt) with that 59pt → 75pt total top inset, which positions the first
  line ~75pt below the textView origin.
- The visual "ghost" letters at the very top of the title are the floating toolbar's translucent
  background overlay (`background(.regularMaterial)` style) — not text clipping. This is the
  expected UI affordance: the toolbar floats above content while remaining semi-transparent.
- Cross-format coverage holds: same `TXTTextViewBridge` is used by MD scroll mode and the
  chunked TXT path; all three call sites wrapped in `GeometryReader` in this PR.
- The `NativeTextPagedView` (paged MD path) has the same hardcoded `textContainerInset` flaw
  but is OUT OF SCOPE for bug #179 (bug filed for TXT only). Track as follow-up if reported.

## Artifacts

- `dev-docs/verification/artifacts/bug-179-postmerge-txt-di-clear-20260514.png` — post-merge
  screenshot showing title clearly below DI on the merged build (v3.21.31).
- Pre-FIXED simulator-verify screenshots: `bug-179-prefix-01..06-*-20260514.png` (sequential
  reset / seed / open / settle workflow against the working-tree binary).

## Code references

- Fix commit: 306e54f (`fix(#611): TXT Dynamic Island clipping — wire safe-area top inset through TXT bridges`)
- Merge commit: 8c08df7 (PR #631)
- Audit log: `.claude/codex-audits/fix-issue-611-txt-dynamic-island-clipping-audit.md` (manual-fallback, ship-as-is)
- Unit tests: `vreaderTests/Views/Reader/TXTTextViewBridgeSafeAreaInsetTests.swift` (4 tests for `combinedTextInset`)
