---
kind: feature
id: 12
status_target: VERIFIED
commit_sha: 9b98d71c8084739268c80b24b323114d66e8947c
app_version: 3.21.1 (build 278)
date: 2026-05-13
verifier: claude
device_or_simulator: iPhone 17 Pro Simulator
os_version: iOS 26.5
build_configuration: Debug
backend: n/a
result: pass
---

## Acceptance criteria

| Criterion | Observed | Result |
|---|---|---|
| (a) open .md file → TOC list shows headings (not empty state) | `MDTOCVerificationTests.testMDTOCShowsHeadingsAndNavigatesOnTap` step 5–6: `tocEmptyState` absent + `tocRow-1ebd0fb0...Introduction:0` button found — confirms TOCBuilder.forMD extracted headings from real file | PASS |
| (b) tap heading → panel dismisses (navigation wired) | Step 7–8: tap `tocRow-1ebd0fb0...:Introduction:0` → `annotationsPanelSheet` disappeared within 5s — confirms `AnnotationsPanelView.handleNavigate` fired `onNavigate(locator) + onDismiss()` | PASS |
| (c) reader still alive after navigation (no crash) | Step 9: `readerBackButton` existed after panel dismiss | PASS |

## Commands run

```bash
# Run MD TOC verification UITest
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild test \
  -project vreader.xcodeproj -scheme vreader \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
  -only-testing:vreaderUITests/MDTOCVerificationTests

# Result: ** TEST SUCCEEDED **
# Test Case '-[vreaderUITests.MDTOCVerificationTests testMDTOCShowsHeadingsAndNavigatesOnTap]'
#   passed (14.394 seconds).
```

## Observations

**New seed infrastructure**: `--seed-md-toc` launches with `TestSeeder.seedMDWithTOC` — writes a real MD file
(5 headings: one H1, three H2, one H3) to `ImportedBooks/` and inserts a `BookRecord` titled
"Test Markdown TOC". The file is generated in-memory (no DebugFixtures bundle resource required),
following the same pattern as `seedPositionTest`.

**tocRow ID contains heading content**: The first tocRow button's accessibility identifier was
`tocRow-1ebd0fb0935a317387c62e8f6eeca89cbc11c57255284cd2443a2b7ac3487490:Introduction:0` —
this is the UUID-format TOCEntry ID that `TOCListView` builds from `entry.id`. The title "Introduction"
and sequence index 0 confirm it's the first heading in the fixture.

**Panel dismiss confirms onNavigate + onDismiss wired**: `AnnotationsPanelView.handleNavigate` calls
`onNavigate(locator)` then `onDismiss()`. Panel dismissal is the observable signal that the full
navigation chain fired — the MDReaderContainerView's `onNavigate` closure calls
`viewModel.updateScrollPosition(charOffsetUTF16:)`, which drives the UITextView scroll.

**TOC accessible on first open without chrome toggle**: The annotations button was visible immediately
after the reader loaded (no chrome-toggle tap needed). The `app.tap()` fallback was not exercised.

**Round-2 completes the full acceptance surface**: The 2026-05-07 evidence file (`result: partial`)
covered 138 tests across 19 unit suites pinning the data-layer behavior (heading extraction,
UTF-16 offsets, fenced-code-block skipping, round-trip locators). This round closes the deferred
UI gesture leg end-to-end. All acceptance criteria now pass.

## Artifacts

No screenshots taken — all assertions are via deterministic `XCTNSPredicateExpectation` checks
(element existence + disappearance). The `.xcresult` bundle is at:
`~/Library/Developer/Xcode/DerivedData/vreader-hdhlhcqmxppsadhececcxeadpkvz/Logs/Test/Test-vreader-2026.05.13_02-49-09-+0800.xcresult`
